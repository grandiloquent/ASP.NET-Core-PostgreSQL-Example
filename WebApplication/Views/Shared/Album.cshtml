@model WebApplication.Models.Video

<input name="original.Album.Id" value="@Model.Album?.Id" type="hidden"/>
<input name="original.Album.Cover" value="@Model.Album?.Cover" type="hidden"/>
<input name="original.Album.Title" value="@Model.Album?.Title" type="hidden"/>

<input type="hidden" asp-for="Album.Id"/>
<div class="form-group">
    <label asp-for="Album.Cover"></label> <input asp-for="Album.Cover" class="form-control"/>
</div>
<div class="form-group">
    <label asp-for="Album.Title"></label> <input asp-for="Album.Title" class="form-control"/>
</div>
